import linux commands

ls - list
cd - change directory
mv filename.blah /new_folder/ - move a file to a place
cp filename.blah /new_folder/ - copy a file to a place
mkdir directoryname - make a new directory

--------

git commands

git clone ________ - copies stuff down from the internet/github

git status - checks current directory for changes

git add .

git commit -am'some comment'

git push origin master

git push origin gh-pages

git pull origin gh-pages

-----------
Order to push new code:

git Status //to see if there are changes
git commit -am'What'djado' //to upload / log changes
git push origin gh-pages //to push to the a website

git add . // to add new files


-------

** before making local changes ALWAYS do:**
git pull origin gh-pages

git checkout <branchname> //sets your branch to be something new

gitclone <urlfromgithub> //copies things from internet/github.com

